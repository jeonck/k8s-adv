# Mermaid Test

This is a test mermaid diagram:

```mermaid
graph TD
    A[Start] --> B[End]
```

If you see a diagram above, your viewer supports Mermaid.
If you see code blocks, your viewer doesn't support Mermaid.
